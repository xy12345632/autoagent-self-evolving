# AutoAgent 快速开始

## 环境准备

### 系统要求

- Python 3.10 或更高版本
- pip 或 pipenv
- Windows/macOS/Linux

### 检查Python版本

```bash
python --version
# 或
python3 --version
# 确保显示 3.10+
```

## 安装步骤

### 1. 克隆项目

```bash
cd "自进化个人AI Agent完整项目包"
```

### 2. 创建虚拟环境（推荐）

```bash
# 使用venv
python -m venv venv

# 激活虚拟环境
# Windows:
venv\Scripts\activate
# macOS/Linux:
source venv/bin/activate
```

### 3. 安装依赖

```bash
# 使用requirements.txt
pip install -r requirements.txt

# 或使用pyproject.toml安装
pip install -e .
```

### 4. 验证安装

```bash
# 检查autoagent命令
autoagent --version

# 或运行测试
python -c "from autoagent import AutoAgent; print('OK')"
```

## 配置API密钥

### 获取API密钥

你需要从以下平台获取API密钥:

1. **OpenCode Zen**: https://opencode.ai
2. **OpenRouter**: https://openrouter.ai

### 配置方式

#### 方式1: 编辑配置文件

编辑 `autoagent/config/config.yaml`:

```yaml
opencode_zen_api_key: sk-your-opencode-zen-key
openrouter_api_key: sk-or-v1-your-openrouter-key

model_providers:
  opencode_zen:
    endpoint: https://opencode.ai/zen/v1
    default_model: minimax-m2.5-free
  openrouter:
    endpoint: https://openrouter.ai/api/v1
    default_model: openai/gpt-4o-mini
```

#### 方式2: 设置环境变量

```bash
# Windows PowerShell
$env:OPENCOD EZEN_API_KEY="sk-your-key"
$env:OPENROUTER_API_KEY="sk-or-v1-your-key"

# macOS/Linux
export OPENCOD EZEN_API_KEY="sk-your-key"
export OPENROUTER_API_KEY="sk-or-v1-your-key"
```

## 启动AutoAgent

### 基本启动

```bash
# 使用CLI命令
autoagent

# 或直接运行
python autoagent/main.py
```

### 首次运行

首次启动时，系统会:

1. 创建必要目录
2. 初始化记忆文件
3. 发现内置工具
4. 显示欢迎界面

```
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
  🤖 AutoAgent - 自进化个人AI Agent
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

欢迎使用AutoAgent！这是您的个人AI助手。

功能特性：
  🧠 持久记忆 - 跨会话记忆您的偏好和知识
  📚 技能系统 - 自动学习和创建新技能
  🛠️ 40+工具 - 网页搜索、代码执行、文件操作等
  💬 多平台 - CLI交互
  🔄 自进化 - 越用越聪明

常用命令：
  /help    - 显示帮助
  /clear   - 清除对话历史
  /memory  - 查看记忆状态
  /skills  - 查看已学技能
  /exit    - 退出程序

>>>
```

## 基本使用

### 对话交互

直接在提示符后输入内容:

```
>>> 你好，今天天气怎么样？
>>> 帮我写一个Python的快速排序算法
>>> 搜索一下最新的AI新闻
```

### 命令行命令

| 命令 | 说明 | 示例 |
|------|------|------|
| `/help` | 显示帮助 | `/help` |
| `/clear` | 清除对话历史 | `/clear` |
| `/memory` | 查看记忆状态 | `/memory` |
| `/skills` | 查看已学技能 | `/skills` |
| `/exit` | 退出程序 | `/exit` |
| `/skill create` | 创建新技能 | `/skill create` |
| `/skill list` | 列出技能 | `/skill list` |
| `/history` | 查看历史 | `/history` |
| `/stats` | 使用统计 | `/stats` |

### 多行输入

使用 Ctrl+D (Linux/macOS) 或连续空行结束:

```
>>> 第一行
第二行
第三行
^D
```

## 常见场景

### 场景1: 编程助手

```
>>> 帮我写一个Python的装饰器用于计时
>>> 解释一下什么是闭包
>>> 帮我debug这段代码
```

### 场景2: 信息查询

```
>>> 搜索一下Python 3.12的新特性
>>> 今天有什么AI领域的新闻
>>> 帮我查一下以太坊当前价格
```

### 场景3: 文件操作

```
>>> 帮我创建一个README.md
>>> 读取当前目录下的文件列表
>>> 分析一下这个CSV文件
```

### 场景4: 任务自动化

```
>>> 每天早上9点提醒我检查邮件
>>> 设置一个每周一的定时任务
>>> 创建子代理帮我分析代码
```

## 高级配置

### 启用其他平台

编辑 `config.yaml`:

```yaml
gateway:
  telegram:
    enabled: true
    bot_token: "your-telegram-bot-token"
  discord:
    enabled: true
    bot_token: "your-discord-bot-token"
```

### 配置工具权限

```yaml
tools:
  terminal:
    enabled: true
    allowed_commands:
      - ls
      - git
      - npm
      - python
  code_execute:
    enabled: true
    sandbox: docker  # docker/none
```

### 配置记忆存储

```yaml
memory:
  type: sqlite_fts
  db_path: ./data/memory.db
  max_entries: 10000
  auto_summarize: true
```

## 故障排除

### 问题1: 导入错误

```
ModuleNotFoundError: No module named 'autoagent'
```

**解决**: 重新安装包

```bash
pip install -e .
```

### 问题2: API密钥错误

```
Error: Invalid API key
```

**解决**: 检查 `config.yaml` 中的密钥配置

### 问题3: 依赖安装失败

```
ERROR: Could not install package
```

**解决**: 升级pip

```bash
pip install --upgrade pip
```

### 问题4: 端口被占用

```
OSError: [Errno 98] Address already in use
```

**解决**: 结束占用端口的进程或配置其他端口

### 问题5: 权限错误

```
PermissionError: [Errno 13] Permission denied
```

**解决**: 以管理员权限运行或修改文件权限

## 卸载

```bash
# 删除虚拟环境
rm -rf venv

# 或卸载包
pip uninstall autoagent
```

## 下一步

- 阅读 [特性.md](特性.md) 了解详细功能
- 阅读 [架构.md](架构.md) 了解系统架构
- 阅读 [配置指南.md](配置指南.md) 进行高级配置
- 阅读 [工具系统.md](工具系统.md) 了解更多工具
